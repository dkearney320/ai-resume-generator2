services:
  - type: web
    name: ai-resume-generator
    env: node
    plan: free
    envVars:
      - key: NODE_VERSION
        value: 18
    buildCommand: |
      npm ci
      npm run build
    startCommand: npm start
